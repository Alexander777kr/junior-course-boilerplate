import React from 'react';
import classes from './index.module.css';

const MainHeader = () => {
    return (
        <h1 className={classes.mainHeader}>Список товаров</h1>
    );
};

export default MainHeader;